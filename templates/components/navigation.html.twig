<nav class="nav-fixed">
    <div class="h-20 bg-[#3E3E53] shadow-2xl" aria-label="{{ translations.translate('nav.aria_label') }}">
        <div class="container mx-auto h-full px-0 flex items-center justify-center space-x-40 portrait:hidden">
            <a href="{{ language == 'fr' ? '/' : '/en' }}" class="interactive text-2xl font-bold {% if current_path == '/' or current_path == '/en' %}text-[#658c79]{% else %}hover:text-[#658c79] transition-colors{% endif %}">
                {{ translations.translate('nav.projects') }}
            </a>
            <a href="{{ language == 'fr' ? '/contact' : '/contact-en' }}" class="interactive text-2xl font-bold {% if current_path == '/contact' or current_path == '/contact-en' %}text-[#658c79]{% else %}hover:text-[#658c79] transition-colors{% endif %}">
                {{ translations.translate('nav.contact') }}
            </a>
            
            {% if request.isAuthenticated() %}
                <a href="/admin" class="interactive text-2xl font-bold {% if current_path starts with '/admin' %}text-[#658c79]{% else %}hover:text-[#658c79] transition-colors{% endif %}">
                    {{ translations.translate('nav.admin') }}
                </a>
                <a href="/logout" class="interactive text-2xl font-bold hover:text-[#658c79] transition-colors">
                    {{ translations.translate('nav.logout') }}
                </a>
            {% endif %}
        </div>
        
        {# Mobile navigation #}
        <div class="container mx-auto h-full px-0 py-12 flex items-center justify-center space-x-10 landscape:hidden">
            <a href="{{ language == 'fr' ? '/' : '/en' }}" class="interactive text-2xl font-bold {% if current_path == '/' or current_path == '/en' %}text-[#658c79]{% else %}hover:text-[#658c79] transition-colors{% endif %}">
                {{ translations.translate('nav.projects') }}
            </a>
            <a href="#" class="interactive text-2xl font-bold opacity-0">
                ha
            </a>
            <a href="{{ language == 'fr' ? '/contact' : '/contact-en' }}" class="interactive text-2xl font-bold {% if current_path == '/contact' or current_path == '/contact-en' %}text-[#658c79]{% else %}hover:text-[#658c79] transition-colors{% endif %}">
                {{ translations.translate('nav.contact') }}
            </a>
        </div>

        {# Underline for active nav item #}
        {% if current_path == '/' or current_path == '/en' %}
            <div class="fixed top-[calc(60px)] left-[calc(50%-10px-10rem)] w-[90px] h-1 bg-white portrait:hidden"></div>
        {% elseif current_path == '/contact' or current_path == '/contact-en' %}
            <div class="fixed top-[calc(60px)] left-[calc(63%-14px-10rem)] w-[93px] h-1 bg-white portrait:hidden"></div>
        {% elseif current_path starts with '/admin' %}
            <div class="fixed top-[calc(60px)] left-[calc(75%-10px-10rem)] w-[80px] h-1 bg-white portrait:hidden"></div>
        {% endif %}
        
        {# Language switcher - desktop #}
        <div class="fixed top-10 right-[3vw] transform -translate-y-1/2 flex space-x-2 hidden landscape:flex portrait:hidden">                
            <a href="{{ language == 'en' ? current_path|replace({'/en': '/', '/contact-en': '/contact'}) : current_path }}" class="interactive text-3xl {{ language == 'fr' ? '' : 'opacity-50 hover:opacity-100 transition-opacity' }}" aria-label="Version française" lang="fr">🇫🇷</a>                
            <span class="text-2xl" aria-hidden="true">/</span>                
            <a href="{{ language == 'fr' ? current_path|replace({'/': '/en', '/contact': '/contact-en'}) : current_path }}" class="interactive text-3xl {{ language == 'en' ? '' : 'opacity-50 hover:opacity-100 transition-opacity' }}" aria-label="English version" lang="en">🇬🇧</a>            
        </div>
        
        {# Language switcher - mobile #}
        <div class="fixed bottom-2 right-[3vw] transform -translate-y-1/2 flex space-x-2 hidden portrait:flex landscape:hidden">                
            <a href="{{ language == 'en' ? current_path|replace({'/en': '/', '/contact-en': '/contact'}) : current_path }}" class="interactive text-3xl {{ language == 'fr' ? '' : 'opacity-50 hover:opacity-100 transition-opacity' }}" aria-label="Version française" lang="fr">🇫🇷</a>                
            <span class="text-2xl" aria-hidden="true">/</span>                
            <a href="{{ language == 'fr' ? current_path|replace({'/': '/en', '/contact': '/contact-en'}) : current_path }}" class="interactive text-3xl {{ language == 'en' ? '' : 'opacity-50 hover:opacity-100 transition-opacity' }}" aria-label="English version" lang="en">🇬🇧</a>             
        </div>  

        {# Logo #}
        <div class="absolute top-0 left-1/2 transform -translate-x-1/2 portrait:hidden">
            <img src="/assets/img/AshaLogo.png" alt="Logo Asha" class="w-24 h-24 portrait:w-16 portrait:h-16">
        </div>
        <div class="absolute top-[calc(18px)] left-1/2 transform -translate-x-1/2 landscape:hidden">
            <img src="/assets/img/AshaLogo.png" alt="Logo Asha" class="w-24 h-24 portrait:w-20 portrait:h-20">
        </div>
    </div>
</nav>
